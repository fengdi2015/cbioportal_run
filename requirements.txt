# Runtime dependencies for the cbio project.
# On Linux, install these first with:
#   python3 -m pip install -r requirements.txt
# Then install the local project in editable mode:
#   python3 -m pip install -e .

anndata>=0.12
h5py>=3.16
matplotlib>=3.10
numpy>=2.4
pandas>=2.3
requests>=2.33
scipy>=1.17
